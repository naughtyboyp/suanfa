#include<stdio.h>
int power(int a, int b)
{
    int ret = 1;
    while(b)
    {
        if(b%2 == 1)
        {
            ret = ret*a;
        }
        a = a*a;
        b/2;
    }
    return ret;
}
